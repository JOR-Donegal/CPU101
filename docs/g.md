# Modern CPU Design
Most of your computing experience has probably been on Intel/AMD type processors.
As a family, these are _Complex Instruction Set Computers_ (CISC). 
The design uses complex hardware to execute complex instructions over many clock cycles.
The processors are relativly big and power-hungry.
